[pytest]
; Insert backend/ (this file's directory) onto sys.path so `from domain...`,
; `from adapters...`, `from api...` imports work regardless of how/where
; pytest is invoked from, without needing __init__.py packages under tests/.
pythonpath = .
